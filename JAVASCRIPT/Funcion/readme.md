// como crear un funcion(function), y el bucle si(if)

function par_o_impar (numero) {
    if (numero % 2 === 0 ){
        return "par"
    } else {
        return "impar"
    }
}

console.log (par_o_impar(35))
